#ifndef _GENERAL_UTILS_
#define _GENERAL_UTILS_


/*
  create the directory to save the data in and
  the directory to use for mounting the filesystem
*/
int create_private_directories(char * path, char * directory_name);



#endif