mkdir - 

rename - 

open - decrypt file

// when all file descriptors are closed, release is called
release - encrypt file


// opens a directory, return dirent structure that can be used in readdir
opendir 

// reads each entry (files, directories) in the dirent structure from opendir
// dirent struct:
  //
    struct dirent {
      long d_ino;  inode number 
      off_t d_off;  offset to the next dirent 
      unsigned short d_reclen;  length of this record 
      unsigned char d_type;  type of file 
      char d_name[256];  filename 
    };
  //
readdir

// implement decrypt and encryption function
